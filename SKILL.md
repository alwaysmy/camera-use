# SKILL.md — 用这套相机观察物理世界

> 配套 MCP 使用。MCP 工具描述说明"**能做什么**"；本文说明"**怎么用好**"，尤其是那些
> 不写出来一定会踩的坑。

## 三个典型工作流

### A. 看一眼现在什么情况
```
camera_observe {what:"summary"}      先拿一句话（有没有人/在哪/多亮）
camera_look    {view:"fuse"}         需要"真的看一眼"时再取图（暗光下 fuse 最清楚）
```
**别一上来就 `camera_look`** —— 返回图片很占上下文。先用 `summary` 判断要不要看图。

### B. 刚才发生了什么 / 谁来过
```
camera_observe {what:"events"}       最近 60 条事件（带 HH:MM:SS）
```
事件**只在状态变化时记录**：有人进入/离开、人脸增减、手势变化、采集切换、暗场校准。
没有事件 = 这段时间状态没变（**不代表相机坏了**）。

### C. 要结构化信息（谁在哪、手比了什么）
```
camera_observe {what:"person"}       人物存在/位置/覆盖度/运动量（IR差分+运动，无模型）
camera_observe {what:"face"}         人脸框：原生 Viola-Jones + 旁路 YOLO（坐标+置信度）
camera_observe {what:"hand"}         手部 21 点 + 五指手势（"五指张开"/"剪刀手"/"竖大拇指"…）
camera_observe {what:"pose"}         人形 + 17 点骨架
```
坐标**一律是像素**，原点左上。旁路结果带 `vision_age_ms`：**超过 ~1000ms 就别当实时用**。

## 五个必须避开的坑

| 坑 | 现象 | 规避 |
|---|---|---|
| **未压缩码流 + IR 同开** | RGB 掉到 **0 fps**（USB 带宽到顶，非故障） | 要么不用未压缩（用 `mjpg`），要么只开一路。`config` 会自动拒绝并说明 |
| **IR 相机被独占** | 打不开 IR，或打开了取不到帧 | Windows Hello 开着时 IR 被占。**同一台机器的 IR 同一时刻只能一个进程用** |
| **暗场校准必须遮镜头** | 不遮就校准 → 把正常画面当成暗场，越校越黑 | 校前**用不透光物完全盖住**。校准时后端会校验亮度，>32 直接拒绝 |
| **相机白平衡与灯光绑定** | 换灯/关灯后画面固定偏色，怎么调软件白平衡都不对 | 灯光会变 → 用**自动**档；灯光稳定 → 手动校准一次，**变了要重校** |
| **环境光太亮时 IR 差分失效** | `led_on - led_off < 12` | 此时补光差分/人物检测的 IR 分量不可信，只能靠运动检测 |

## 参数怎么选

| 想看什么 | 用哪个 view |
|---|---|
| 暗光下看清人和物 | `fuse`（RGB 出颜色 + IR 出亮度） |
| 可见光原图 | `rgb` |
| 红外黑白（不受可见光影响） | `ir` |
| **只被红外照亮的东西**（皮肤、强反红外物） | `diff`（亮帧−灭帧） |
| 对焦/配准/细节 | `edge`（IR 增强的边缘图） |
| 暗场残留（那块灰/彩是不是校准没盖住） | `darkmap` |
| 配准对不对（对了是细轮廓，整片亮=没对上） | `alignerr` |

## 数字怎么读

| 字段 | 含义 |
|---|---|
| `led_on` / `led_off` | IR 补光灯亮/灭帧的平均亮度。**差值 <12 → 环境光太亮，IR 差分不可靠** |
| `cast_a` / `cast_b` | LAB 色偏（截尾均值）。趋近 0 = 画面中性；b 正偏黄、负偏蓝 |
| `vision_age_ms` | 旁路推理结果的新鲜度。>1000ms 说明旁路卡了或没跑 |
| `yolo_ms` | 人脸+骨架+手部一次推理耗时（GPU 上实测 20~60ms） |
| `coverage`（person） | 活跃网格占比，粗略反映"占画面多大" |
| `motion` / `ir_diff` | 运动像素占比 / IR 差分能量。静止的人靠 ir_diff，动的人靠 motion |

## 控制相机时注意

- `camera_control {config:{...}}` 会**真正改变物理设备状态**，且会重置采集（约 1~2 秒不出帧）
- 改分辨率/码流前先想清楚带宽：未压缩码流上不了 720p 以上，且不能再带 IR
- 调完等 1~2 秒再 `camera_observe` 验证（相机生效有延迟）
